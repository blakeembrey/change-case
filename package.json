{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "ts-scripts install"
  },
  "devDependencies": {
    "@borderless/ts-scripts": "^0.15.0",
    "@vitest/coverage-v8": "^1.0.1",
    "typescript": "^5.2.2",
    "vitest": "^1.0.1"
  }
}
